# Claude Code 任務：喝水頁 v3 成品感改版

## 目標

請改進 `lib/pages/water_page.dart` 的視覺與互動，讓喝水頁從「功能可用」提升到「成品 App 感」。

這次重點是 UI/UX 與動畫，不要重寫整個資料邏輯。

## 目前問題

目前喝水頁已經有：

- 每杯容量 `water_cup_ml`
- 每日目標 `water_goal_ml`
- 今日杯數 `water_yyyy-MM-dd`
- 依身高/體重/生日/性別/活動量快速建議目標
- 動態水瓶與水位
- 首頁「喝足夠的水」習慣連動

但視覺上仍有問題：

- 水瓶素材質感高，但頁面其他 UI 比較工程感，整體不夠一致。
- 上方資訊卡像資料卡，缺少設計感。
- 水滴 dots 如果杯數多會顯得雜亂。
- 右上角同時有 tune 和 settings，視覺偏擠。
- 主按鈕功能清楚，但還不夠有「我喝水了」的行動感。
- 水位目前畫在瓶身上方，能看到，但不夠像真的在瓶子裡。

## 不要做的事

- 不要改 `SharedPreferences` key 名稱：
  - `water_cup_ml`
  - `water_goal_ml`
  - `water_yyyy-MM-dd`
  - `water_goal_date`
- 不要移除首頁喝水習慣連動。
- 不要改 `main.dart` 的 tab 結構，除非必要。
- 不要改 onboarding、家庭頁、體重頁。
- 不要引入新的狀態管理套件。
- 不要大範圍重構無關程式。
- 不要把喝水頁做成深色或海洋遊戲風；維持柔和、乾淨、陪伴型 App 感。

## 要做的改動

### 1. 整體佈局

保留目前大方向：

- 上方 AppBar
- 上方摘要區
- 中央水瓶主視覺
- 下方進度/杯數
- 底部主要操作按鈕

但請調整成更有層次：

- 背景使用柔和水藍漸層。
- 加入非常淡的水波紋/氣泡/光斑裝飾，但不要搶主體。
- 中央水瓶應該像主視覺，而不是孤立圖片。
- 水瓶後方可加淡淡圓形光暈或展示台陰影。

### 2. AppBar

目前右上有 tune 和 settings 兩個 icon，視覺偏擠。

請改成：

- AppBar 右上只保留全域設定 icon，進 `SettingsPage`。
- 喝水設定入口移到摘要卡或小 chip，例如：
  - `每杯 250 ml`
  - 點擊後開啟喝水設定 bottom sheet
- AppBar 標題使用深藍綠色，不要硬藍。
- AppBar 背景透明。

### 3. 摘要卡

目前顯示：

```text
750 / 2000 ml
3 / 8 杯 · 每杯 250 ml
目前已補充...
```

請改成更像設計稿：

```text
今日補水
750 ml
目標 2000 ml · 還差 1250 ml
[每杯 250 ml]  [建議目標]
```

要求：

- 主數字 `750 ml` 要大，但不要過大。
- `目標 / 還差` 比主數字小一階。
- `每杯 250 ml` 做成 pill/chip，點擊開設定。
- `建議目標` 可以直接開設定 sheet，或在設定 sheet 裡套用建議。
- 達標時文案改成：
  - `今日補水完成`
  - `已達標，可以繼續少量補充`

### 4. 水瓶主視覺

目前使用：

- `assets/images/water/bottle_overlay.png`
- Flutter `_WaterFillPainter`

請保留這個 asset，但調整顯示方式：

- 水瓶尺寸可以略大一點，但不要擠壓上下資訊。
- 水位要對齊瓶身內部，不能外溢。
- 水位顏色要更明顯，但仍保持玻璃感。
- 水面可以有一條波浪線。
- 水位上升時應有平滑動畫。
- 按下「喝了一杯」時，水瓶可做輕微彈跳或冒泡動畫。

如果你認為單張 `bottle_overlay.png` 無法做出真正前後分層，可以先用目前做法改善，不要重新生成素材。未來再拆成：

- bottle_back.png
- water layer
- bottle_front.png

### 5. 杯數進度

目前 dots 會依杯數顯示，杯數多時容易雜亂。

請改成更穩的做法：

選項 A，推薦：

- 顯示最多 8 個主要水滴節點。
- 若目標杯數超過 8，以比例映射到 8 個節點。
- 例如 12 杯目標時，不要真的顯示 12 個水滴。

選項 B：

- 保留 dots，但若超過 12 個，改成進度條 + 文字。

請選較穩定、較美觀的方案。

### 6. 底部按鈕

目前主按鈕：

```text
喝了一杯 (250 ml)
```

請改成更自然：

```text
我喝了一杯
250 ml
```

或：

```text
記一杯
250 ml
```

要求：

- 主按鈕要明顯，是頁面最重要 CTA。
- 減少一杯按鈕保留，但視覺弱一點。
- 不要讓減號按鈕跟主按鈕搶焦點。
- 點擊主按鈕時，水瓶最好有輕微反應。

### 7. 喝水設定 bottom sheet

保留現有設定功能：

- 每杯容量
- 每日目標
- 快速套用建議目標
- 合理範圍驗證

但視覺可以整理：

- 標題：`喝水設定`
- 欄位：`每杯容量`、`每日目標`
- 建議卡：`建議 xxxx ml`
- 說明文字保留，但簡短。

不要刪掉警告/驗證。

### 8. 首頁連動

請確認以下仍正常：

- 喝水達標時，首頁「喝足夠的水」習慣會自動完成。
- 在首頁勾選「喝足夠的水」時，喝水頁會同步到達標杯數。
- 在首頁取消時，喝水頁能恢復之前杯數。
- 動態目標使用 `water_goal_ml / water_cup_ml` 計算，不要回到固定 8 杯。

## 視覺風格

參考目前首頁方向：

- 柔和、乾淨、可愛
- 不要硬藍色 AppBar
- 不要 Material demo 感
- 不要卡片堆卡片
- 不要太花、不要海洋遊戲風
- 目標是「陪伴型健康工具」，不是醫療儀表板

推薦色彩：

- 深文字：`#17657A` 或相近深藍綠
- 主水色：cyan / sky blue
- 背景：非常淡的藍白漸層
- 達標：柔和綠或金色點綴

## 驗收標準

請完成後確認：

- `flutter analyze` 通過
- `flutter test` 通過
- 喝水頁可正常開啟
- 設定每杯容量和每日目標可儲存
- 關掉/重開頁面後設定仍保留
- 加一杯/減一杯正常
- 水位會跟著變化
- 達標時首頁習慣連動仍正常
- UI 不溢出小螢幕
- 主按鈕文字不被截斷

## 建議實作順序

1. 先整理 AppBar 與設定入口。
2. 改摘要卡資訊層級。
3. 調整水瓶展示區與水位位置。
4. 改 dots/進度呈現。
5. 改底部 CTA。
6. 跑 `flutter analyze` 和 `flutter test`。
7. 最後再微調間距與字級。

## 備註

如果有不確定的美術取捨，請優先選：

- 簡潔
- 柔和
- 成品感
- 可讀性
- 和首頁兔咪房間風格一致

不要為了炫技加入過度動畫。
